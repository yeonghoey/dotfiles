#!/bin/bash

pull () {
  git -C "$1" pull > /dev/null || echo "Failed to pull \"$1\""
}

pull "$HOME/.dotfiles"
pull "$HOME/.spacemacs.d"

pull "$HOME/repos/notes"
git -C "$HOME/repos/notes" lfs pull
