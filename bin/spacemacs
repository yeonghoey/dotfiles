#!/bin/bash

if [[ "$(uname)" == "Linux" ]]; then
  dropbox.py start
fi

pull () {
  echo "Pull \"$1\""
  git -C "$1" pull
  echo
}

pull "$HOME/repos/yeonghoey"
pull "$HOME/repos/notes"
pull "$HOME/.spacemacs.d"
pull "$HOME/.dotfiles"

emacs &
